---
# This playbook will install mysql and create db user and give permissions.

- name: Install Mysql package
  yum: name={{ item }} state=installed
  with_items:
   - mysql-server
   - MySQL-python
   - libselinux-python
   - libsemanage-python

- name: Create Mysql configuration file
  copy: src=my.cnf.j2 dest=/etc/my.cnf force=yes
  notify:
  - restart mysql


- name: Configure SELinux to start mysql on any port
  seboolean: name=mysql_connect_any state=true persistent=yes
  when: sestatus.rc != 0

- name: Start Mysql Service
  service: name=mysqld state=started enabled=true

  # copy mysql data dir (/var/lib/mysql/) into host mounted drive (/mysql-data)
- name: Copy mysql data to shared drive
  shell:
    cp -apr /var/lib/mysql/* /mysql-data
  ignore_errors: True



#- name: insert iptables rule
#  lineinfile: dest=/etc/sysconfig/iptables state=present regexp="{{ mysql_port }}"
#              insertafter="^:OUTPUT " line="-A INPUT -p tcp  --dport {{ mysql_port }} -j  ACCEPT"
#  notify: restart iptables

#- name: Create Application Database
#  mysql_db: name={{ dbname }} state=present
#  ignore_errors: True

- name: Create Application DB User
  mysql_user: name={{ dbuser }} password={{ upassword }} state=present
  notify:
    - restart mysql
  ignore_errors: True


